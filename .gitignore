# Google Sheets API Private Credentials
instance/credentials.json

# Local Runtime SQLite Database (Active Instance Store)
instance/app.db

# Database backup files (should never be tracked)
instance/*.backup_*

# Pipeline backup files (safety copies, not primary data)
*_backup.csv

# Dynamic Bytecode and Environment Cache
__pycache__/
*.pyc
.env

# Local development test scripts (not part of core test suite)
smoke_test.py
nul
