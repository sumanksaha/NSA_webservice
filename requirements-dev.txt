# =============================================================================
# NSA Webservice — Development Dependencies (shim)
# =============================================================================
# The single source of truth is pyproject.toml [project.optional-dependencies].
# This file exists so CI cache keys (validation.yml) and muscle-memory
# `pip install -r requirements-dev.txt` keep working.
#
# Runtime deps:  pip install -e .
# Dev deps:      pip install -r requirements-dev.txt
#                (equivalent to: pip install -e ".[dev]")
# Optional ML:   pip install -e ".[all]"
# =============================================================================

-e ".[dev]"
