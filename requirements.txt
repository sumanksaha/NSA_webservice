# =============================================================================
# NSA Webservice — Runtime Dependencies
# =============================================================================
# This file is kept for reference/CI convenience.
# The single source of truth for runtime dependencies is pyproject.toml
# under [project.dependencies].
#
# To install runtime deps:
#   pip install -e .
#
# To install dev deps as well:
#   pip install -e ".[dev]"   (once dev extras are configured)
#   -- or --
#   pip install -r requirements-dev.txt
#
# Last synced: July 2026
# =============================================================================
# Build/tooling pinned for Render compatibility (avoid 3.14 pip/setuptools issues)
# These must resolve first so the editable build below uses compatible setuptools.
# ponytail: order matters - before -e . which triggers the isolated build.
setuptools>=70,<85
wheel
pip>=26.2.1
-e .
