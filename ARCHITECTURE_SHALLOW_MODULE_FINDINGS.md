## Review — Shallow Module Deepening (AGENTS.md §8 + task.md D1–D9)

**Evidence sources:** AGENTS.md §8 (lines 280–340), task.md Deepening §D1–D9 (lines 882–1171), code at `app/shared/case_resolver.py`, `app/services/document_lifecycle.py`, `app/pdf_assembly/engine.py`, `app/inspection/photo_service.py`, `app/shared/document_case_manager.py`, `app/services/backup_restorer.py`, `app/utils/sync.py`, `app/utils/lookup.py`, `app/annexure/routes.py`, `app/evidence/routes.py`, plus live pytest runs (46 passed, 0 failed).

> **Status update (2026-09-09):** D7 ✅ done (`audit_logger` factory, wrappers deleted, 23/23 tests). D8: module ✅ deepened 2026-09-08 (`0bfc8d3`) but **callers not migrated — 6 call sites raise at runtime**. D1–D6 entries below unchanged.

### Completed deepening (depth increased — verified)

- **D1 CaseResolver** (`app/shared/case_resolver.py`: 109L) — interface `resolve(case_id, kind) -> ResolvedCase | None`; 7 tests pass (`test_case_resolver.py`). **Locality:** high (replaces inline lookups in 5 blueprints). **Depth:** 1→4.
- **D2 DocumentSaveCoordinator** (`app/services/document_lifecycle.py`: 210L) — encapsulates 5 private helpers (`_resolve_case`, `_save_document_content`, `_log_audit`, `_snapshot_version`, `_actor`) into `save()`; 12 tests pass (`test_document_lifecycle.py`). **Depth:** 2→4.
- **D3 PDFAssemblyEngine** (`app/pdf_assembly/engine.py`: 785L) — consolidated from `pdf_utils.py` grab-bag; backward-compat shims preserved; interface `assemble/post_process/embed_photos/generate_from_html`. **Depth:** 3→4. ⚠️ **No dedicated `test_pdf_assembly_engine.py` found** — validation relies on integration only.
- **D4 InspectionPhotoService** (`app/inspection/photo_service.py`: 503L) — extracted EXIF/validation/storage/OCR/geo from `photo_routes.py`; 28 tests pass (`test_inspection_photo_service.py`). **Depth:** 1→4.
- **D5 DocumentCaseManager** (`app/shared/document_case_manager.py`: 772L) — parameterized `(model, template_dir, bp, case_type, sections_fn)`; eliminates ~1,500L duplication across case_file_generator + adjudication routes. **Depth:** 2→4. ⚠️ **No dedicated `test_document_case_manager.py` found**.
- **D6 BackupRestorer** (`app/services/backup_restorer.py`: 280L) — unified the 3 triplicated restore pipelines (`restore_from_airtable_csv`/excel/sheets) + dead `sync_to_sheets()` + 3 byte-identical maps into one deep module with a single canonical `BACKUP_MODULE_TO_TABLE` and `BackupRestorer` adapter class. **Interface:** `restore_from(target) -> int`, `restore_if_empty() -> dict`, `auto_restore_if_empty() -> dict`. **Tests:** 46 pass (`test_priority7_redundancy.py` 21 + `test_backup_targets.py` 7 + `test_full_archive_backup.py` 18). **Locality:** high (all restore logic in one module). **Leverage:** high (deletes dead code, removes 3× copy-paste). **Depth:** 1→4. **Test surface:** migrated from patching 7 private internals to patching the public `BackupRestorer` interface — one adapter, one mock surface.

### Remaining shallow modules (updated 2026-09-09)

- **D7 audit callers — ✅ DONE (2026-09-09).** `app/services/audit_context.py`: `audit_logger(entity_type).log(entity_id, action, *, actor=None, **details)` — binds `entity_type`, normalizes `actor` (authenticated username; `"anonymous"` when unauthenticated/inactive/outside request context; explicit override wins), swallows writer failures at WARNING. The 9-line `_log_audit` wrappers + duplicate `_actor()` helpers deleted from `annexure/routes.py` (5 call sites → `_audit = audit_logger("annexure")`) and `evidence/routes.py` (3 call sites); `document_lifecycle._log_audit` delegates to `AuditLogger(case_type)` (dynamic per-save binding). **Mock surface:** `app.services.audit_context._default_writer` resolves at call time — one patch point for every instance; `tests/test_document_lifecycle.py` migrated to it with assertions unchanged. **Tests:** `tests/test_audit_context.py` (13 tests: binding, actor normalization incl. raising `current_user`, swallowing at both writer layers, end-to-end hash chain via `verify_audit_chain`) — 23/23 pass (pytest shim; pytest unavailable in env). **Depth:** 1→3. Core `log_audit` untouched. Step-4 direct callers migrated the same day (adjudication ×3, photo_service ×3, document_case_manager ×2, api/routers ×1, search/routes ×1) — entity-type strings preserved byte-identically as hash-chain keys. Out of scope by design: the four RAG logger modules (self-contained wrappers with their own patch surfaces) and the `app/inspection/audit.py` re-export shim.
- **D8 `app/utils/lookup.py` — module ✅ deepened (2026-09-08, commit `0bfc8d3`); callers ⚠️ NOT migrated (BROKEN).** The module now matches the spec: `LookupResult` dataclass (`found`/`error`/`data`, legacy `as_tuple()` shim), pure `repair_kmc_json()`, injectable `RateLimiter` (fcntl + timestamp) and `DefaultKmcHttpClient` (TLS `SECLEVEL=1`, cookie-warming); `lookup_ce` no longer raises; FSSAI half Postgres-migrated 2026-08-25. **But the interface change was never propagated:** all 6 production call sites still use the old contracts — `result, error = lookup_fssai(...)` → `TypeError` (not iterable); CE routes `try/except` + `jsonify(result)` → not JSON-serializable. Affected: `case_file_generator/routes.py:410`, `adjudication/routes.py:300,313`, `inspection/routes/lookup_routes.py:22,53`, `sample/routes.py:152,214`, `fbo_issue/routes.py:128,133`. CI stayed green because `tests/test_lookup_fssai_postgres.py` exercises the module directly — the caller seam was never tested (interface-is-the-test-surface violation). **Next:** migrate the 6 sites to `if result.error / elif result.found` + add `tests/test_lookup.py` caller-contract tests; then D8 reaches 1→4.
- **D9 `verification_service.py`** — NOT a deepening candidate (task.md §Deepening Strategy Notes). **Seam:** 3 sub-modules (`geo`, `ip`, `distance`) each have justified adapter split (production HTTP vs test mock) — merging would destroy independent testability. **Recommendation:** optional 1-line `_degrade()` helper only.

### Vocabulary-aligned assessment
- **Depth:** D1–D7 reached target (3→4, 2→4, 1→3). D8 module reached 1→4 (2026-09-08) but callers still speak the dead tuple contract, so effective depth at the seam is 1 until migration.
- **Locality:** D7 realized (all 3 wrapper duplicates deleted; actor normalization lives in one module). D8 is spread (6 call sites / 5 blueprints) — exactly what its caller migration must touch.
- **Leverage:** D6 > D7 > D8 — D6 deletes dead code (zero production impact confirmed by grep); D7 removes 3 identical wrappers and collapses the test mock surface to one point; D8's leverage is blocked behind caller migration.
- **Seam / Adapter:** D4, D9 have justified seams (photo service = EXIF/validation/storage/OCR/geo split; verification = 3 external services). D6 has none — just delete. D7 shipped as a factory seam (`AuditLogger` + call-time-resolved writer). D8's 3 adapter seams (`RateLimiter`, HTTP adapter, JSON repair pure function) shipped in the module; the caller-facing contract (`LookupResult`) has only one adapter so far — the callers themselves are the missing second side of the seam.

### Residual risks
- No dedicated test file for PDFAssemblyEngine or DocumentCaseManager (integration-only coverage).
- **D8 callers are broken at runtime** (tuple-unpack `TypeError` on FSSAI routes; CE `jsonify(LookupResult)` also raises) — caller migration + caller-contract tests are the top deepening follow-up.
- D1–D7 verified green (D7: 23/23 audit + lifecycle tests via a pytest shim, 2026-09-09 — pytest is not installed in this env; re-verify with `pytest tests/test_audit_context.py tests/test_document_lifecycle.py`).